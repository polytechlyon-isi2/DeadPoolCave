{% extends 'layout.html.twig' %}

{% block title %}Profil{% endblock %}

{% block content %}

<h2 class="text-center">{{ block('title') }}</h2>
<h2 class="text-center">{{ app.user.username }}</h2>
<h2 class="text-center">{{ app.user.role }}</h2>
<h2 class="text-center"><a href="{{ path('profilEdit', { 'id': user.id }) }}" class="btn btn-info btn-lg" title="Edit your profil">Edit your profil  <span class="glyphicon glyphicon-edit"></span></a></h2>
{% endblock %}